# 西北农林科技大学硕博通用开题报告 LaTeX 模板格式说明

## 1. 编译方式

推荐使用：

```bash
latexmk -xelatex main.tex
```

本模板使用 `biblatex + biber`。`latexmk` 会自动完成 XeLaTeX -> Biber -> XeLaTeX 的必要迭代。

Windows 也可双击 `compile.bat`；Linux/macOS 可运行 `./compile.sh`。

## 2. 学位层次与封面信息

- `settings/degree.tex`：设置 `doctor` / `master`。
- `settings/info.tex`：填写年级、中英文题目、学院、学科、姓名学号、导师、开题论证组、日期。
- 中英文长题目在学校封面版心内自动换行并居中；题目区、基本信息区、日期区位置相互独立，长题目不会推动后部信息区。
- 论证小组成员：普通空格分隔姓名，`\\` 手工决定换行。

## 3. 正文与目录

- 从目录开始继承 `nwafuthesis` 的 A4 版心、页眉页脚、章/节标题、正文、图表和分页规则。
- 封面使用开题报告专用布局，不改变后续正文 `geometry`。
- 目录自动分页，不使用手工点线或固定坐标。

## 4. 正文引用格式

普通正文文献引用统一使用：

```latex
\parencite{key}
```

输出示例：

```text
（视觉上为半角圆括号）(Gao et al., 2023)
(农业农村部, 2025)
(Gao et al., 2023; Wang et al., 2024)
```

即“作者 + 年份”整体置于括号内。正文现有引用已经统一改为 `\parencite`。

只有确实需要叙述式写法时才使用：

```latex
\textcite{key}
```

其效果为 `Gao et al. (2023)`，当前开题正文默认不采用这种写法。

## 5. 参考文献列表

- 作者-年份制，不编号，不采用 GB/T 7714 数字制。
- 参考文献采用**一个连续列表**输出：先中文、后英文，中文与英文交界处**不增加任何额外段间距**，间距与普通相邻条目完全一致。
- 中文文献按作者/机构拼音排序；同一作者/机构再按年份、题名排序。
- 英文文献按第一作者姓氏字母顺序排序；同一作者再按年份、题名排序。
- 参考文献条目全部使用常规字形，不人为加粗、斜体；“参考文献”章标题仍沿用正文章标题格式。
- URL/DOI 允许合理断行，避免越出版心。

## 6. 参考文献数据库维护

中文条目应设置（`sortkey` 使用作者/机构拼音，用于中文组内部排序）：

```bibtex
langid = {chinese},
sortname = {对应作者或机构的拼音排序串},
sortkey = {对应作者或机构的拼音排序串},
```

英文条目统一设置 `langid = {english}`，由正常作者姓氏排序。

## 7. 主要源文件

- `main.tex`：文档入口与章节、参考文献输出顺序。
- `nwafuthesis.cls`：正文/目录/参考文献基础格式。
- `settings/format.tex`：开题封面与专用格式。
- `settings/info.tex`：用户填写信息。
- `settings/degree.tex`：硕士/博士切换。
- `settings/packages.tex`：附加宏包及表格列型。
- `references.bib`：参考文献数据库。
- `contents/*.tex`：正文各章节。
